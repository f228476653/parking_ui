import { EventEmitter, OnInit } from '@angular/core';
import { MatDialog } from '@angular/material';
import { ITime } from '../w-clock/w-clock.component';
import { WTimeComponent } from "../w-time/w-time.component";
export declare class MatTimepickerToggleComponent implements OnInit {
    private dialog;
    userTime: ITime;
    userTimeChange: EventEmitter<ITime>;
    color: string;
    timepicker: WTimeComponent;
    constructor(dialog: MatDialog);
    ngOnInit(): void;
    readonly time: string;
    showPicker($event: any): boolean;
    private emituserTimeChange();
}
