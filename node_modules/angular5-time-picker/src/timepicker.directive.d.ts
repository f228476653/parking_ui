import { ElementRef } from '@angular/core';
import { WTimeComponent } from "./time-control/w-time/w-time.component";
export declare const MAT_TIMEPICKER_VALUE_ACCESSOR: any;
export declare const MAT_TIMEPICKER_VALIDATORS: any;
export declare class MatTimepickerInputEvent {
    /** Reference to the timepicker input component that emitted the event. */ target: TimepickerDirective;
    /** Reference to the native input element associated with the timepicker input. */
    targetElement: HTMLElement;
    /** The new value for the target timepicker input. */
    value: string;
    constructor(/** Reference to the timepicker input component that emitted the event. */ target: TimepickerDirective, 
        /** Reference to the native input element associated with the timepicker input. */
        targetElement: HTMLElement);
}
export declare class TimepickerDirective {
    private _elementRef;
    timepicker: WTimeComponent;
    readonly value: string;
    constructor(_elementRef: ElementRef);
}
